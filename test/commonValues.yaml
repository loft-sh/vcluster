syncer:
  image: REPLACE_IMAGE_NAME
  env:
  - name: DEBUG
    value: "true"
  resources:
    requests:
      cpu: "0"

vcluster:
  resources:
    requests:
      cpu: "0"

etcd:
  resources:
    requests:
      cpu: "0"

controller:
  resources:
    requests:
      cpu: "0"

api:
  resources:
    requests:
      cpu: "0"
