#replicas: 1
integrations:
  metricsServer:
    enabled: true
    nodes: true
    pods: true

#sync:
#  nodes:
#    enabled: false
#    # If nodes sync is enabled, and syncAllNodes = true, the virtual cluster
#    # will sync all nodes instead of only the ones where some pods are running.
#    syncAllNodes: true

controlPlane:
  serviceMonitor:
    enabled: true # no matches for kind "ServiceMonitor" in version "monitoring.coreos.com/v1"
