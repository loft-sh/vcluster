controlPlane:
  statefulSet:
    image:
      registry: ""
      repository: {{ .Repository }}
      tag: {{ .Tag }}
sync:
  fromHost:
    gatewayClasses:
      enabled: true
      selector:
        matchLabels:
          e2e.vcluster.loft.sh/gatewayclass: gatewayapi-vcluster
  toHost:
    services:
      enabled: true
    configMaps:
      enabled: true
      all: true
    secrets:
      enabled: true
      all: true
    gatewayApi:
      gateways:
        enabled: true
      httpRoutes:
        enabled: true
      referenceGrants:
        enabled: true
      tlsRoutes:
        enabled: true
      backendTLSPolicies:
        enabled: true
