# vendor/ is generated by `go mod vendor` and contains upstream documentation
# examples (Docker swagger specs, Tailscale api.md, go-openapi CI config) that
# secret scanning flags as credentials. They are not our secrets and cannot be
# rotated by us. Reviewed 2026-08-28; revisit if vendoring stops.
paths-ignore:
  - "vendor/**"
